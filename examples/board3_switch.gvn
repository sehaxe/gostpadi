# switch (как на доске): колонка на каждую ветку — слева, по центру, справа;
# подписи веток стоят над первой плиткой своей колонки
ввод scanf("%d", &a)
если switch (a)
    1: printf(y); break
    2: printf(z); break
    иначе: a = 10; break
